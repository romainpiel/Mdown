extends layout

block content
    textarea#editor hey

    script(type='text/javascript')
        var editor = CodeMirror.fromTextArea(document.getElementById("editor"), {
            mode: 'markdown',
            matchBrackets: true,
            lineWrapping: true,
            theme: "default"
        });
        
    script
        var socket = io.connect('http://localhost');
        socket.on('news',socket.emit('my other event', { my: 'data' }));