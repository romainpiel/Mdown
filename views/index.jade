extends layout

block content
    textarea#editor hey

    script(type='text/javascript')
        var editor = CodeMirror.fromTextArea(document.getElementById("editor"), {
            mode: 'markdown',
            matchBrackets: true,
            lineWrapping: true,
            theme: "default"
        });
    